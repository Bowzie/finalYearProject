define(function () {
    //Empty constructor
    function audioEncode() {

    }

    audioEncode.prototype = {   
        wavEncode: function () {
            if(wavEncoder !== null && pcmData != null)
            {
                //Encode
            }
            else
            {
                return null;
            }
        }
    };  

    return charting;
});
